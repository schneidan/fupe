{
  "name": "@fupe/db",
  "version": "0.1.0",
  "private": true,
  "description": "FUPE database migrations and graph utilities (PostgreSQL + Apache AGE)",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "reset": "node scripts/reset.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
