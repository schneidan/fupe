DATABASE_URL=postgresql://fupe:fupe_dev@localhost:5433/fupe
# Used by docker compose for Postgres init (must match DATABASE_URL password).
# On servers: set a strong value in this gitignored .env — do not edit docker-compose.yml.
POSTGRES_PASSWORD=fupe_dev
# On a shared VPS, also set (staging compose only):
# STAGING_POSTGRES_PASSWORD=change-me-staging
API_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3001
# OPENROUTER_API_KEY=sk-or-...     # IMAGE vision + server STT (enable ZDR in OR privacy settings)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# VISION_MODEL=google/gemini-2.5-flash-lite
# STT_MODEL=openai/whisper-large-v3-turbo
# OPENROUTER_HTTP_REFERER=https://fupe.app
# OPENROUTER_APP_TITLE=FUPE
# JWT_SECRET=change-me
# Email: console (API logs) or resend / smtp
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=re_...
# EMAIL_FROM=FUPE <noreply@fupe.app>
# EMAIL_PROVIDER=smtp
# SMTP_HOST=127.0.0.1
# SMTP_PORT=1025
# EMAIL_FROM=FUPE <noreply@fupe.local>
# AUTO_VERIFY_EMAIL=true
# BOOTSTRAP_MODERATOR_EMAIL=you@example.com
# REQUIRE_API_KEY=false
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_DEVELOPER=price_...
# STRIPE_PRICE_BUSINESS=price_...
