{
  "name": "fupe",
  "version": "0.1.0",
  "private": true,
  "description": "Find Ultimate Parent Entity — corporate ownership transparency",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:reset-volume": "docker compose down -v",
    "db:migrate": "pnpm --filter @fupe/db migrate",
    "db:setup": "docker compose up -d postgres && pnpm db:migrate",
    "db:reset": "pnpm --filter @fupe/db reset",
    "ingest": "pnpm --filter @fupe/ingest ingest",
    "ingest:schedule": "pnpm --filter @fupe/ingest ingest:schedule",
    "test:smoke": "pnpm --filter @fupe/api test && pnpm exec ts-node --transpile-only --compiler-options '{\"module\":\"commonjs\",\"moduleResolution\":\"node\",\"esModuleInterop\":true}' apps/web/src/lib/safe-next.smoke.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
